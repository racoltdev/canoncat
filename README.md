canoncat takes no options, only a list of files. It discovers the canonical name of any arguments and prints the contents of the source file for each.

Run install.sh then restart shell (or source canoncat_completions.sh) to setup.
